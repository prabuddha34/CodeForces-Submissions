fun main() {
    val (a, x, y) = readLine()!!.split(" ").map { it.toInt() }
 
    if (x < 0 || x > a || y < 0 || y > a) {
        println(2)
    } else if (x == 0 || x == a || y == 0 || y == a) {
        println(1)
    } else {
        println(0)
    }
}